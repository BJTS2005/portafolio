---
import Layout from "../layouts/Layout.astro";
---

<Layout title="404 - Página en construcción">
    <!-- Efectos de fondo -->
    <div class="fixed inset-0 overflow-hidden pointer-events-none">
        <div
            class="absolute -top-40 -right-40 w-96 h-96 bg-accent-violet/20 rounded-full blur-[128px] animate-pulse-glow"
        >
        </div>
        <div
            class="absolute -bottom-40 -left-40 w-96 h-96 bg-accent-cyan/10 rounded-full blur-[128px] animate-pulse-glow"
            style="animation-delay: 1s;"
        >
        </div>
    </div>

    <main class="min-h-screen flex items-center justify-center px-6">
        <div class="text-center max-w-lg">
            <!-- Glitch effect number -->
            <div class="relative mb-8">
                <h1
                    class="text-[150px] md:text-[200px] font-bold leading-none bg-gradient-to-r from-accent-cyan via-accent-purple to-accent-violet bg-clip-text text-transparent"
                >
                    404
                </h1>
                <div
                    class="absolute inset-0 text-[150px] md:text-[200px] font-bold leading-none text-accent-purple/20 blur-xl"
                >
                    404
                </div>
            </div>

            <!-- Icon -->
            <div
                class="w-20 h-20 mx-auto mb-6 bg-surface/50 border border-white/10 rounded-2xl flex items-center justify-center"
            >
                <span class="text-4xl">🚧</span>
            </div>

            <!-- Message -->
            <h2
                class="text-2xl md:text-3xl font-semibold text-text-primary mb-4"
            >
                Página en <span class="text-accent-purple">construcción</span>
            </h2>

            <p class="text-text-secondary mb-8 leading-relaxed">
                <span class="text-accent-cyan font-mono">&gt;</span>
                Esta sección aún está siendo desarrollada. Vuelve pronto para ver
                las novedades.
            </p>

            <!-- Terminal style message -->
            <!-- Terminal style message -->
            <div
                class="bg-surface/50 border border-white/10 rounded-xl p-4 mb-8 font-mono text-sm"
            >
                <div class="flex items-center gap-2 mb-3">
                    <span class="w-3 h-3 rounded-full bg-red-500/80"></span>
                    <span class="w-3 h-3 rounded-full bg-yellow-500/80"></span>
                    <span class="w-3 h-3 rounded-full bg-green-500/80"></span>
                </div>
                <div class="text-left">
                    <p class="text-text-muted">
                        <span class="text-accent-cyan">$</span> cd /página-solicitada
                    </p>
                    <p class="text-red-400">
                        bash: cd: /página-solicitada: No existe el directorio
                    </p>
                    <p class="text-text-muted">
                        <span class="text-accent-cyan">$</span> ./construir_pagina.sh
                    </p>
                    <p class="text-red-400">
                        bash: ./construir_pagina.sh: Permiso denegado
                    </p>
                    <p class="text-text-muted">
                        <span class="text-accent-cyan">$</span> chmod +x construir_pagina.sh
                    </p>
                    <p class="text-text-muted">
                        <span class="text-accent-cyan">$</span> ./construir_pagina.sh
                    </p>
                    <p class="text-accent-purple">
                        [INFO] Iniciando construcción de página...
                    </p>
                    <p class="text-yellow-400">
                        [WARN] Página aún no disponible
                    </p>
                    <p class="text-text-secondary">[....] Trabajando en ello</p>
                    <p class="text-text-muted flex items-center">
                        <span class="text-accent-cyan">$</span>
                        <span class="ml-1 w-2 h-4 bg-accent-cyan animate-pulse"
                        ></span>
                    </p>
                </div>
            </div>
            <!-- Back button -->
            <a
                href="/"
                class="inline-flex items-center gap-2 px-8 py-4 bg-gradient-to-r from-accent-cyan to-accent-purple rounded-lg font-medium text-primary hover:scale-105 hover:shadow-[0_0_40px_rgba(0,217,255,0.3)] transition-all"
            >
                <svg
                    class="w-5 h-5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                </svg>
                Volver al inicio
            </a>
        </div>
    </main>
</Layout>
